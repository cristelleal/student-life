{
  "name": "student-life",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/mobile",
    "apps/backend/api",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "format": "turbo format"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
